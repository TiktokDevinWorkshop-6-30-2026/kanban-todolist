{
  "name": "daily-task-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Client-side Kanban Daily Task Tracker with optional Devin session integration served by a zero-dependency Node backend proxy.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
